{
  "name": "ridgesun-website",
  "version": "1.0.0",
  "description": "Ridgesun PureRing industrial air filtration system website",
  "main": "src/index-homepage.html",
  "scripts": {
    "dev": "live-server src --port=3000",
    "build": "echo 'No build step required for static HTML'",
    "deploy": "wrangler pages deploy src --project-name=ridgesun-website",
    "test": "echo 'No tests specified'",
    "lint": "echo 'No linting configured'",
    "format": "prettier --write 'src/**/*.{html,css,js}'"
  },
  "keywords": [
    "industrial",
    "air-filtration",
    "static-site",
    "cloudflare-pages"
  ],
  "author": "Ridgesun",
  "license": "Proprietary",
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.0.0",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}